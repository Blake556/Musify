<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

    <link rel="icon" type="image/x-icon" href="favicon.ico?">

    <link rel="stylesheet" href="/css/style.css">

    <title>Search Musicfy</title>
</head>
<body>

        <h1 class='header'>Search Musify</h1>

        <form action="/" method="post">
            <div class="input-group mb-3 search-bar">
                <button class="btn btn-success" type="submit" id="search" >Search</button>
                <input type="text" class="form-control search-bar" placeholder="Search Artist" aria-label="Example text with button addon" aria-describedby="button-addon1" name="search" required>
            </div>
        </form>
        
        <div class='results-container'>

            <h3 class='result-title'>Results</h3>

            <div class='results'>
                <p> <%= locals.song1 || 'song' %> </p>
                <span class='artist' ><%= locals.artist1 || 'artist' %></span> | <span class='album' ><%= locals.album1 || 'album' %></span>
            </div>

            <div class='results'>
                <p> <%= locals.song2 || 'song' %> </p>
                <span class='artist' ><%= locals.artist1 || 'artist' %></span> | <span class='album' ><%= locals.album2 || 'album' %></span>
            </div>

            <div class='results'>
                <p> <%= locals.song3 || 'song' %> </p>
                <span class='artist' ><%= locals.artist1 || 'artist' %></span> | <span class='album' ><%= locals.album3 || 'album' %></span>
            </div>

            <div class='results'>
                <p> <%= locals.song4 || 'song' %> </p>
                <span class='artist' ><%= locals.artist1 || 'artist' %></span> | <span class='album' ><%= locals.album4 || 'album' %></span>
            </div>

            <div class='results'>
                <p> <%= locals.song5 || 'song' %> </p>
                <span class='artist' ><%= locals.artist1 || 'artist' %></span> | <span class='album'><%= locals.album5 || 'album' %></span>
            </div>

            <div class='results'>
                <p> <%= locals.song6 || 'song' %> </p>
                <span class='artist' ><%= locals.artist1 || 'artist' %></span> | <span class='album' ><%= locals.album6 || 'album' %></span>
            </div>

            <div class='results'>
                <p> <%= locals.song7 || 'song' %> </p>
                <span class='artist' ><%= locals.artist1 || 'artist' %></span> | <span class='album' ><%= locals.album7 || 'album' %></span>
            </div>

            <div class='results'>
                <p> <%= locals.song8 || 'song' %> </p>
                <span class='artist' ><%= locals.artist1 || 'artist' %></span> | <span class='album' ><%= locals.album8 || 'album' %></span>
            </div>

            <div class='results'>
                <p> <%= locals.song9 || 'song' %> </p>
                <span class='artist' ><%= locals.artist1 || 'artist' %></span> | <span class='album' ><%= locals.album9 || 'album' %></span>
            </div>

            <div class='results bottom-result'>
                <p> <%= locals.song10 || 'song' %> </p>
                <span class='artist' ><%= locals.artist1 || 'artist' %></span> | <span class='album'><%= locals.album10 || 'album' %></span>
            </div>
        </div>

    </div>
    
</body>
</html>